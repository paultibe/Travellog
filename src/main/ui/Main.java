package ui;

public class Main {
    public static void main(String[] args) {
//        try {
//            new DestinationDatabaseApp();
//        } catch (FileNotFoundException e) {
//            System.out.println("Unable to run application: file not found");
//        }

        MyFrame myFrame = new MyFrame();

        // JMenu for different tabs
        // look at alarm system for action performed and buttons
        // grid layout for panels?
        // when you open file: a new panel pops up with buttons: save, load, add, print
        // each button is linked to an action performed function. each button opens a new panel so you can enter stuff
        // change jtextfield to string -> Jtextfield.gettext(
    }
}
